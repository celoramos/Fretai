# Perfil de teste: H2 em memória imitando PostgreSQL, para rodar sem Docker.
# As mesmas migrations do Flyway rodam aqui, então erro de schema quebra o teste.
spring:
  datasource:
    url: jdbc:h2:mem:fretai;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
